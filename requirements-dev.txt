# Development / CI dependencies.
#
# Install the runtime stack first, then this file:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# CI NOTE: .github/workflows/deploy.yml currently installs `pytest httpx`
# unpinned, because the Arena GitHub App token has no 'workflows' permission
# and cannot push workflow edits. To pin CI to this file, change those two
# lines in the workflow to one:
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# (Everything else in the workflow is already current: checkout@v7,
# setup-python@v7, flyctl-actions/setup-flyctl@v1 - Node 24 ready.)
#
# These are NOT installed in the production images (Dockerfile.api /
# .worker / .streamlit) — test tooling has no business in a deploy artifact.

-r requirements.txt

pytest==9.1.1
httpx==0.28.1          # REQUIRED by fastapi.testclient.TestClient (starlette
                       # imports it lazily; without it every API test errors
                       # at collection with "RuntimeError: The starlette.
                       # testclient module requires the httpx package")

# --- Optional ML extras, pinned so CI can exercise the ML code paths ---
# The app degrades gracefully without these (see requirements.txt), but the
# full test suite only reaches the classifier/NER branches when they exist.
# Uncomment, or install in a dedicated "ml" CI job, to exercise those paths.
# The pretrained NER test also needs en_core_web_sm. See the README's dated
# verification results; use pytest's actual summary for current pass/skip counts.
# scikit-learn==1.9.0
# spacy==3.8.16
